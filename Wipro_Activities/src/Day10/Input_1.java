package Day10;
import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class Input_1 {
    public static void main(String[] args) throws IOException {
    	/*
        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));

        System.out.print("Enter a string: ");
        String input = reader.readLine();  // reads a full line from the keyboard

        System.out.println("\nYou entered: " + input);
        System.out.println("Character and ASCII values:");

        for (int i = 0; i < input.length(); i++) {
            char ch = input.charAt(i);              // get each character
            int ascii = (int) ch;                   // get ASCII value
            System.out.println(ch + " --> " + ascii);
        }
        */
    	
    	        BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));

    	        System.out.print("Enter a string: ");
    	        String input = reader.readLine();  // Reads the full string

    	        System.out.println("You entered: " + input);
    	    }
    	

    }


